services:
  - type: worker
    name: telegram-astrology-bot
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: FREEASTRO_API_KEY
        sync: false
